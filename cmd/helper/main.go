// Package main is the entry point for kasten-frs-web helper.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "kasten-frs-web helper: not yet implemented")
	os.Exit(1)
}
